# n8n workflow

Import `miniv-local-model-benchmark.json`. The workflow validates fixed fields and calls the public CLI wrapper. Keep endpoint and credential values in an untracked config/environment; the workflow contains no secrets or benchmark logic.
